<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./sidebar.css" />
    <link rel="stylesheet" href="stylesheets/bootstrap.min.css" />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <title>ACCESS CONTROL</title>
    <style>
      /* Sidebar fixa ocupando toda a altura da tela */
      .sidebar {
        position: fixed;
        top: 0;
        left: 0;
        width: 250px; /* Largura da sidebar */
        height: 100vh; /* Altura da sidebar igual à altura da tela */
        padding-top: 20px;
        background-color: #007bff;
        color: white;
      }

      /* Espaço para o conteúdo, para ele não sobrepor a sidebar */
      .content {
        margin-left: 250px; /* Espaço correspondente à largura da sidebar */
        padding: 20px;
        margin-top: 60px; /* Espaço para não ficar embaixo da navbar */
      }
    </style>
  </head>
  <body></body>
</html>
